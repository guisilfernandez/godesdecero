package main

import (
	"github-cursos/godesdecero/mapas"
)

func main() {
	/*estado, texto := variables.ConviertoaTexto(1588)
	fmt.Println(estado)
	fmt.Println(texto)

	if os := runtime.GOOS; os == "Linux." || os == "OS X." || os == "darwin" {
		fmt.Println("Esto no es Windows, esto es", os)
	} else {
		fmt.Println("Esto es Windows")
	}

	switch os := runtime.GOOS; os {
	case "linux":
		fmt.Println("Esto es Linux")
	case "darwin":
		fmt.Println("Esto es Darwin")
	default:
		fmt.Printf("%s \n", os)
	}

	valor, texto := ejercicios.ResultadosEjercicio01("50")
	fmt.Println("Enviado", valor)
	fmt.Println(texto)*/

	//teclado.IngresoNumeros()
	//iteraciones.Iterar()
	//ejercicios.PideNumero()

	//fmt.Println(ejercicios.PideNumero())

	//files.LeoArchivo()
	//funciones.Exponencia(2)
	//arreglosslices.Capacidad()
	mapas.MostrarMapas()
}
